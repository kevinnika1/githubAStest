import os
import logging
import datetime

logger = logging.getLogger(__name__)


def train(credentials, parameters):
    """
    Root training function, customise as needed.

    :param credentials dict: Dictionary of credential dicts in the form {'System Name':{'user':'username', 'password':'password'}}
    :param paramters dict: Dictionary of additional paramters
    """
    
    raise NotImplementedError
